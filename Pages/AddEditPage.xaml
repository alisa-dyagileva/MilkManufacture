<Page x:Class="MilkManufacture.Pages.AddEditPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MilkManufacture.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AddEditPage">

    <Grid Margin="0,51,0,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Логин" Grid.Row="0" Grid.Column="0" Margin="10" VerticalAlignment="Center"/>
        <TextBox x:Name="LoginTextBox" Grid.Row="0" Grid.Column="1" Text="{Binding Login}" MaxLength="100" Margin="10" Height="30"/>

        <TextBlock Text="Роль" Grid.Row="1" Grid.Column="0" Margin="10" VerticalAlignment="Center"/>
        <ComboBox Grid.Row="1" Grid.Column="1" x:Name="ComboRole" DisplayMemberPath="Name" SelectedValuePath="Id" SelectedValue="{Binding RoleId}" Margin="10" Height="30"/>

        <TextBlock Text="Блокировка" Grid.Row="2" Grid.Column="0" Margin="10" VerticalAlignment="Center"/>
        <CheckBox Grid.Row="2" Grid.Column="1" x:Name="CbBlocked" IsChecked="{Binding Blocked}" Margin="10" VerticalAlignment="Center">
            <TextBlock Text="Заблокировать пользователя"/>
        </CheckBox>

        <TextBlock Text="Пароль" Grid.Row="3" Grid.Column="0" Margin="10" VerticalAlignment="Center"/>
        <TextBox Name="PasswordTextBox" Grid.Column="1" Margin="5,5,5,5" Grid.Row="3"/>

        <Button Grid.Row="4" Grid.Column="1" Content="Сохранить" Name="BtnSave" Click="BtnSave_Click" Width="150" Height="30" Margin="10"/>
    </Grid>
</Page>